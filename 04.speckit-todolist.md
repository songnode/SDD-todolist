# GitHub Spec Kit 워크플로우 - 할일 목록 웹앱

Spec-Driven Development(SDD) 방식으로 할일 목록 웹앱을 구축하는 전체 워크플로우입니다.

---

## 🎬 실습 시나리오

**상황**: 사용자가 "할일 목록 앱에 편집 기능을 추가해주세요"라고 요청했습니다.
**목표**: Spec Kit 워크플로우를 따라 편집 기능을 완전히 구현하고 배포 가능한 상태로 만듭니다.

## 0단계: 기준선 확인

**목표**: 기존 애플리케이션이 정상 동작하는지 확인합니다.

Next JS 프로젝트 생성
```bash
npx create-next-app@latest todolist-app --typescript --eslint --tailwind --app --disable-git
```

```bash
npm run dev
# 브라우저에서 http://localhost:3000 접속
```

**예상 실행 결과**
```
▲ Next.js 16.x
- Local:        http://localhost:3000
- ready in 1.2s
```

**확인 포인트**
- [ ] 할일 추가 기능 동작
- [ ] 완료 토글 기능 동작
- [ ] 삭제 기능 동작
- [ ] 새로고침 후 데이터 유지

---

## /speckit.constitution
```bash
Copilot Chat에서:
/speckit.constitution
코드 품질, 테스트 표준, 사용자 경험 일관성 및 성능 요구사항에 중점을 둔 원칙을 수립하십시오.
이러한 원칙이 기술적 결정과 구현 선택을 어떻게 안내해야 하는지에 대한 거버넌스를 포함하십시오.

또는

/speckit.constitution
이 프로젝트는 할일 목록(Todo List) 웹앱입니다.

[개발 원칙]
- Next.js(App Router) + React + TypeScript 로 구성한다
- 스타일링은 Tailwind CSS 만 사용한다
- 컴포넌트는 기능 단위로 분리한다 (TodoInput, TodoItem, TodoList, FilterBar 등)
- 코드에 한국어 주석을 달아 초심자가 이해할 수 있도록 한다
- 모바일과 데스크탑 모두에서 보기 좋게 반응형으로 만든다
- 불필요하게 복잡한 상태 관리 라이브러리는 사용하지 않는다 (React Hooks 만 사용)
- 모든 로그 및 AI 와의 대화는 한국어로 한다

[테스트 원칙]
- 단위 테스트는 Vitest + React Testing Library 로 작성한다
- E2E 테스트는 Playwright 로 작성한다
- implement 완료 후 반드시 /speckit.checklist testing 으로 품질을 검증한다
- 각 수용 기준(Given-When-Then)에 대응하는 테스트 케이스를 1:1 로 작성한다
- 버그 수정 시에는 테스트를 먼저 작성해 실패를 확인한 뒤(Red) 코드를 수정한다(Green)
- 모바일 화면(375px 기준)에서도 동작을 확인한다
```
---

## /speckit.specify — 신규 기능 스펙 작성
**목표**: "할일 편집 기능"을 명확한 스펙 문서로 작성합니다.

**실행 방법**
```
Copilot Chat에서:
/speckit.specify
다음 기능을 가진 할일 목록 웹앱을 만듭니다.

[기능 요구사항]
1. 할일 추가 — 제목(필수)과 설명(선택)을 입력하여 새 할일을 추가한다
2. 할일 완료 체크/해제 — 체크박스를 눌러 완료 상태를 토글한다
3. 할일 삭제 — 삭제 버튼을 눌러 목록에서 제거한다 (삭제 전 확인 없음)
4. 필터링 — 전체 / 진행중 / 완료 세 가지 탭으로 목록을 필터링한다
5. 로컬 스토리지 자동 저장 — 새로고침해도 목록이 유지된다
6. 다크모드 지원 — 우측 상단 토글 버튼으로 라이트/다크 모드를 전환한다
7. 반응형 디자인 — 모바일(375px)과 데스크탑(1280px) 모두에서 자연스럽게 보인다

[수용 기준 예시]
- 제목 없이 추가 버튼을 누르면 "제목을 입력하세요" 메시지가 표시된다
- 할일이 하나도 없으면 "할일이 없습니다" 안내 문구가 표시된다
- 완료된 할일은 제목에 취소선이 표시된다
```

**생성되는 파일**
- 스펙: `/specs/002-edit-todo/spec.md`
- 요구사항 체크리스트: `/specs/002-edit-todo/checklists/requirements.md`

**핵심 확인 포인트**
- [ ] User Story가 우선순위별(P1~P3)로 정의되어 있는가
  - **US1 (P1)**: 기존 할일 편집 (인라인 편집, 저장/취소)
  - **US2 (P2)**: 편집 중 유효성 검증 (빈 제목, 길이 초과 차단)
  - **US3 (P3)**: 편집 중 다른 작업 방지 (버튼 비활성화)
- [ ] Functional Requirements가 테스트 가능한가 (예: FR-001 편집 버튼 표시, FR-005 제목 1~100자/설명 0~500자 검증, FR-011 편집 중 다른 버튼 비활성화)
- [ ] Edge Cases가 명시되어 있는가 (ESC 취소, Enter 저장, textarea Enter=줄바꿈, 완료 항목도 편집 가능, 필터 상태 유지)
- [ ] 구현 세부사항이 배제되어 있는가 (React, hooks, state 등 미언급)

**실습 활동**
```bash
code specs/002-edit-todo/spec.md
code specs/002-edit-todo/checklists/requirements.md
```
---

## /speckit.clarify — 핵심 질문으로 명확화

**목표**: 스펙의 모호한 부분을 질문을 통해 명확히 합니다.

**실행 방법**
```
Copilot Chat에서:
/speckit.clarify
```

**이번 실습에서 반영된 결정사항**

| 질문 | 결정 |
|---|---|
| 편집 모드 진입 방식은? | 각 항목에 별도 "편집" 버튼 추가 (명시적 액션) |
| 다중 줄 설명 편집 시 Enter 키 동작은? | 설명은 textarea, Enter는 줄바꿈, 저장은 버튼만 |
| 편집 중 변경사항 유실 방지 정책은? | ESC/취소만 변경 무시, 다른 동작은 자유롭게 허용 |

**확인 포인트**
- [ ] `spec.md`의 Clarifications 섹션에 결정사항이 기록되었는가
- [ ] 모든 `[NEEDS CLARIFICATION]` 마커가 제거되었는가
- [ ] 결정사항이 User Story와 Requirements에 반영되었는가

---


## /speckit.plan — 구현 계획 및 설계

**실행 방법**
```
Copilot Chat에서:
/speckit.plan

또는

Copilot Chat에서:
/speckit.plan
[기술 스택]
- Next.js (App Router)
- React
- TypeScript
- Tailwind CSS
- React Hooks (useState, useEffect, useContext) 로 상태 관리
- Browser Local Storage 로 데이터 저장
- Vitest + React Testing Library (단위 테스트)
- Playwright (E2E 테스트)
- ESLint + Prettier

[UI/UX]
- Tailwind CSS 를 사용한 모던하고 깔끔한 UI/UX
- 카드 형태의 할일 아이템, 여백을 넉넉하게 사용
- 다크모드는 Tailwind 의 dark: 클래스와 class 전략을 사용한다
```
---

**목표**: 기술 스택, 아키텍처, 설계 산출물을 작성합니다.

**실행 방법**
```
Copilot Chat에서:
/speckit.plan
```

**생성되는 파일**
- 계획: `/specs/002-edit-todo/plan.md`
- 리서치: `/specs/002-edit-todo/research.md`
- 데이터 모델: `/specs/002-edit-todo/data-model.md`
- 계약: `/specs/002-edit-todo/contracts/ui-actions.md`
- 퀵스타트: `/specs/002-edit-todo/quickstart.md`

**핵심 확인 포인트**

`plan.md`
- [ ] 기술 스택이 기존 애플리케이션과 일치하는가 (Next.js 16, React 19, TypeScript 5, Tailwind CSS 4)
- [ ] 구현 전략이 명확한가 (인라인 편집, 기존 `validation.ts` 재사용, `editingId` 상태로 동시 편집 방지)
- [ ] 성능 예산이 정의되어 있는가 (편집 모드 전환 100ms 이내, 저장 후 UI 업데이트 50ms 이내)

`data-model.md`
- [ ] 기존 `TodoItem` 타입에 `updatedAt` 필드가 추가되었는가
- [ ] `EditingState` 관리 방식이 명시되어 있는가

`contracts/ui-actions.md`
- [ ] `updateTodo` 동작이 명확히 정의되어 있는가 (입력: id, title, description / 출력: updatedAt 갱신, 로컬 스토리지 저장)

`quickstart.md`
- [ ] 개발 환경 설정 방법이 명시되어 있는가
- [ ] TDD 구현 순서가 제시되어 있는가
- [ ] 수동 테스트 체크리스트가 포함되어 있는가

> **확인 포인트 ✅**  
> `plan.md` 에 폴더 구조, 컴포넌트 목록, 상태 관리 방식이 구체적으로 나와 있는지 확

---

## /speckit.tasks

**목표**: 구현 가능한 단위 작업으로 분해하고 TDD 순서를 정의합니다.

**실행 방법**
```
Copilot Chat에서:
/speckit.tasks
```

**생성되는 파일**: `/specs/002-edit-todo/tasks.md`

**Phase 구조(예시)**
- [ ] Phase 1-2: Setup & Foundational
- [ ] Phase 3: User Story 1 (P1 - MVP)
- [ ] Phase 4: User Story 2 (P2)
- [ ] Phase 5: User Story 3 (P3)
- [ ] Phase 6: Polish & Cross-Cutting

**예상 출력 (예시)**
```
[ ] Task 1: Next.js 프로젝트 스캐폴딩 (create-next-app, Tailwind 설정)
[ ] Task 2: Todo 타입 정의 (types/todo.ts)
[ ] Task 3: useTodos 커스텀 훅 작성 (추가/토글/삭제 로직)
[ ] Task 4: localStorage 연동 (useEffect 로 저장/불러오기)
[ ] Task 5: TodoInput 컴포넌트 작성
[ ] Task 6: TodoItem, TodoList 컴포넌트 작성
[ ] Task 7: FilterBar 컴포넌트 작성 (전체/진행중/완료)
[ ] Task 8: 다크모드 토글 기능 작성
[ ] Task 9: 반응형 레이아웃 스타일링
[ ] Task 10: Vitest 단위 테스트 작성
[ ] Task 11: Playwright E2E 테스트 작성
```

**TDD 패턴**: 각 Phase마다 "테스트 작성(Red) → 구현(Green)" 순서 (예: T007-T009 테스트 → T010-T017 구현)

**작업 세부사항**
- [ ] 모든 작업에 정확한 파일 경로가 명시되어 있는가 (예: `T010 [US1] updateTodo 함수 구현 in lib/hooks/useTodos.ts`)
- [ ] 검증 방법이 명시되어 있는가 (예: `npm run test:unit -- useTodos.test.tsx`)
- [ ] 병렬 실행 가능한 작업에 `[P]` 마킹이 있는가

**Phase 3 (US1 - MVP) 상세**

```
테스트:
- T007: useTodos 훅 테스트 (updateTodo 함수)
- T008: TodoItem 컴포넌트 테스트 (편집 모드)
- T009: 편집 흐름 통합 테스트

구현:
- T010: updateTodo 함수 구현
- T011: TodoItem 편집 모드 상태 추가
- T012: TodoItem 편집 모드 UI 구현
- T013: 편집 버튼 추가
- T014: 저장 로직 구현
- T015: 취소 로직 구현
- T016: TodoList editingId 상태 관리
- T017: E2E 테스트
```

---

## /speckit.analyze

---

## /speckit.implement

**목표**: `tasks.md`의 작업 순서대로 TDD를 따라 구현합니다.

```
Copilot Chat에서:
/speckit.implement 진행
```

### Phase 3: User Story 1 — 기본 편집 기능 (MVP)

**Step 1: 테스트 작성 (Red)**

```bash
# T007: useTodos 훅 테스트
T007 작업 진행해줘
npm run test:unit -- useTodos.test.tsx
```
**예상 실행 결과 (Red)**
```
FAIL  tests/unit/hooks/useTodos.test.tsx
  ✗ updateTodo() should update title and description
  ✗ updateTodo() should refresh updatedAt timestamp
  Tests: 2 failed, 2 total
```

```bash
# T008: TodoItem 컴포넌트 테스트
T008 작업 진행해줘
npm run test:component -- TodoItem.test.tsx

# T009: 통합 테스트
T009 작업 진행해줘
npm run test:integration -- todo-flow.test.tsx
```

**확인 포인트 (T007-T009)**
- [ ] `updateTodo` 함수 테스트, 제목/설명 업데이트, `updatedAt` 갱신, 로컬 스토리지 저장 검증이 추가되었는가
- [ ] 편집 버튼 클릭 → 편집 모드 전환 / 저장 → `onEdit` 호출 / 취소 → `onCancelEdit` 호출 / Enter·ESC 키 동작 테스트가 있는가
- [ ] 편집→저장→목록 반영, 편집→취소→원래 내용 유지, 편집→저장→스토리지 지속성 검증이 있는가
- [ ] **모든 테스트가 실패(Red)하는가** — 이것이 정상입니다. 아직 구현이 없기 때문입니다.

**Step 2: 구현 (Green)**

```bash
# T010: updateTodo 함수 구현
T010 작업 진행해줘
code lib/hooks/useTodos.ts
npm run test:unit -- useTodos.test.tsx
```
**예상 실행 결과 (Green)**
```
PASS  tests/unit/hooks/useTodos.test.tsx
  ✓ updateTodo() should update title and description
  ✓ updateTodo() should refresh updatedAt timestamp
  Tests: 2 passed, 2 total
```

**확인 포인트 (T010)**
- [ ] `updateTodo` 함수 추가, `id`로 항목 찾아 업데이트, `updatedAt`을 ISO-8601로 갱신, `saveTodos()` 호출, `useTodos` 반환값에 `updateTodo` 포함
- [ ] **T007 테스트가 통과(Green)하는가**

```bash
# T011-T015: TodoItem 컴포넌트 구현 (순차 진행)
T011 작업 진행해줘   # 편집 모드 상태 추가
T012 작업 진행해줘   # 편집 모드 UI 구현
T013 작업 진행해줘   # 편집 버튼 추가
T014 작업 진행해줘   # 저장 로직
T015 작업 진행해줘   # 취소 로직
npm run test:component -- TodoItem.test.tsx
```

**확인 포인트 (T011-T015)**
- [ ] `isEditing`, `editTitle`, `editDescription` 상태 추가
- [ ] 편집 모드일 때 input/textarea 렌더링, 표시 모드일 때 편집 버튼 표시
- [ ] `handleSave`: `onEdit` 호출 후 편집 모드 종료 / `handleCancel`: 원래 값 복원 후 편집 모드 종료
- [ ] Enter/ESC 키 핸들러 구현
- [ ] **T008 테스트가 통과(Green)하는가**

```bash
# T016: TodoList editingId 상태 관리
T016 작업 진행해줘
code components/TodoList.tsx
npm run test:integration -- todo-flow.test.tsx
```

**확인 포인트 (T016)**
- [ ] `editingId` 상태 추가 (`useState<string | null>`)
- [ ] `handleStartEdit`, `handleCancelEdit`, `handleEdit` 구현
- [ ] `TodoItem`에 `isEditing`, `disabled`, `onStartEdit`, `onCancelEdit`, `onEdit` props 전달
- [ ] **T009 통합 테스트가 통과(Green)하는가**

```bash
# T017: E2E 테스트
T017 작업 진행해줘
npm run test:e2e -- todo-core.spec.ts
```

**✅ Checkpoint: User Story 1 완료**
```bash
npm run test -- TodoItem useTodos todo-flow
# 결과: 모두 통과해야 함
```

---

### Phase 4: User Story 2 — 유효성 검증

**Step 1: 테스트 작성 (Red)**
```bash
T018, T019 작업을 순차적으로 진행해줘
npm run test:component -- TodoItem.test.tsx
npm run test:integration -- todo-flow.test.tsx
```
**확인 포인트**: 빈 제목/100자 초과 제목/500자 초과 설명 저장 시도 테스트, 오류 시 `onEdit` 미호출 검증, 테스트가 실패(Red)하는가

**Step 2: 구현 (Green)**
```bash
T020, T021, T022 작업을 순차적으로 진행해줘
npm run test:component -- TodoItem.test.tsx
npm run test:integration -- todo-flow.test.tsx
```
**확인 포인트**: `titleError`/`descriptionError` 상태 추가, `handleSave`에 유효성 검증 함수 연결, 오류 시 저장 차단(return), 오류 메시지 UI 및 필드 테두리(`text-red-500`) 표시, 테스트 통과(Green)

```bash
T023 작업 진행해줘   # E2E 테스트
npm run test:e2e -- todo-core.spec.ts
```

**✅ Checkpoint: User Story 1 AND 2 완료**

---

### Phase 5: User Story 3 — 동시 편집 방지

**Step 1: 테스트 작성 (Red)**
```bash
T024, T025 작업을 순차적으로 진행해줘
npm run test:component -- TodoItem.test.tsx
npm run test:integration -- todo-flow.test.tsx
```
**확인 포인트**: disabled prop 전달 시 버튼 비활성화, 편집 중 다른 항목 버튼 비활성화, 편집 완료 후 재활성화 테스트가 실패(Red)하는가

**Step 2: 구현 (Green)**
```bash
T026, T027, T028 작업을 순차적으로 진행해줘
npm run test:component -- TodoItem.test.tsx
npm run test:integration -- todo-flow.test.tsx
```
**확인 포인트**
- [ ] `TodoItem`에 `disabled` prop 및 편집/삭제 버튼 disabled 속성 추가
- [ ] disabled 시 시각 피드백 (`opacity-50`, `cursor-not-allowed`)
- [ ] ARIA 라벨 추가 (`Edit {todo.title}`, `Save changes`, `Cancel editing`, `Edit title`, `Edit description`)
- [ ] 편집 모드 진입 시 제목 필드 자동 포커스 (`autoFocus`)
- [ ] **모든 테스트가 통과(Green)하는가**

**✅ Checkpoint: 모든 User Story 완료**

---

### Phase 6: Polish & Quality

```bash
npm run lint
npx tsc --noEmit
```
**확인 포인트**: lint 오류 없음, TypeScript 타입 오류 없음

```bash
npm run test -- TodoItem TodoForm useTodos todo-flow filtering --run
```
**예상 실행 결과**
```
✅ TodoItem: 18/18 passed
✅ TodoForm: 3/3 passed
✅ useTodos: 8/8 passed
✅ todo-flow: 6/6 passed
✅ filtering: 1/1 passed
━━━━━━━━━━━━━━━━━━━━━━
✅ Total: 39/39 passed
```

**수동 테스트 체크리스트** (quickstart.md 참조)
- [ ] 편집 버튼 클릭 → 편집 모드 전환
- [ ] 제목/설명 수정 → 저장 → 반영 확인
- [ ] Enter 키로 저장 (제목 필드) / ESC 키로 취소
- [ ] 빈 제목 저장 시도 → 오류 메시지
- [ ] 편집 중 다른 항목 버튼 비활성화 → 편집 완료 후 재활성화

**성능 확인**
- [ ] 편집 모드 전환이 즉시 반응하는가
- [ ] 저장 후 UI 업데이트가 부드러운가

**✅ 최종 확인: 모든 Phase 완료**

---

## ✅ 완료 체크리스트

**Phase 1-2: Setup**
- [ ] 기존 validation 유틸/useTodos 훅/TodoItem 컴포넌트 구조 분석 완료

**Phase 3: US1 (MVP)**
- [ ] T007-T009 테스트 작성 완료 (Red 확인) → T010-T016 구현 완료 → T017 E2E 통과
- [ ] **US1 단독 테스트 가능 확인**

**Phase 4: US2**
- [ ] T018-T019 테스트 작성 완료 (Red 확인) → T020-T022 구현 완료 → T023 E2E 통과
- [ ] **US1 AND US2 동시 동작 확인**

**Phase 5: US3**
- [ ] T024-T025 테스트 작성 완료 (Red 확인) → T026-T028 구현 및 접근성 개선 완료
- [ ] **모든 US 동시 동작 확인**

**Phase 6: Polish**
- [ ] Lint/Typecheck 통과, 전체 테스트 스위트 통과 (39/39), 수동 테스트·성능·문서 완료

### 구현된 기능 요약

1. **인라인 편집**: 편집 버튼 클릭으로 즉시 편집 모드 전환
2. **키보드 단축키**: Enter(제목)=저장, ESC=취소, Enter(설명)=줄바꿈
3. **실시간 유효성 검증**: 빈 제목 차단, 제목 100자/설명 500자 제한
4. **동시 편집 방지**: 한 항목 편집 중 다른 항목 버튼 비활성화
5. **접근성**: ARIA 라벨, 자동 포커스, 키보드 네비게이션
6. **데이터 지속성**: 로컬 스토리지 자동 저장

---

### Checklist — 구현 품질 검증 (implement 후)

> **[품질 게이트]** — `implement` 완료 후 생성된 코드가 `spec.md` 의 모든 수용 기준을 충족하는지  
> `constitution.md` 의 `[테스트 원칙]` 을 기준으로 점검한다.

**Copilot Chat** 에 아래를 입력한다.

```
/speckit.checklist testing
생성된 코드가 spec.md 의 7가지 기능 요구사항과 수용 기준을 모두 충족하는지
테스트 시나리오 관점에서 점검해줘.
constitution.md 의 [테스트 원칙] 도 함께 반영해서 체크리스트를 만들어줘.
누락되거나 구현되지 않은 기능이 있으면 알려줘.
결과를 .specify/specs/001-todo-list/checklists/testing.md 로 저장해줘.
```

**터미널** 에서 확인한다.

```bash
cat .specify/specs/001-todo-list/checklists/testing.md
```

`[ ]` (미통과) 항목이 있으면 아래 순서로 처리한다.

```
1. Copilot Chat 에서 수정 방법을 물어본다
   예) "할일 추가 시 빈 제목 검증이 안 되는 것 같아. 어디를 수정해야 해?"
2. Copilot 에게 해당 컴포넌트/훅 파일 수정을 요청한다
3. 체크리스트를 재실행하여 통과 여부를 확인한다
   예) "/speckit.checklist testing"
4. 모든 항목 통과 → Part 6 (실행 및 테스트) 로 진행
```

> **확인 포인트 ✅**  
> `checklists/testing.md` 가 생성되고, 7가지 기능이 모두 통과 표시되면 완료.

---

## 결과물 실행 및 테스트

### 1. 의존성 설치 (implement 단계에서 자동 설치되지 않은 경우)

```bash
npm install
```

### 2. 개발 서버 실행

```bash
npm run dev
```

예상 출력:

```
▲ Next.js 14.x.x
- Local:        http://localhost:3000

✓ Ready in 1.2s
```

브라우저에서 `http://localhost:3000` 에 접속하여 화면을 확인한다.

### 3. 기능 수동 확인

| 확인 항목 | 방법 |
|---|---|
| 할일 추가 | 제목 입력 후 추가 버튼 클릭 → 목록에 즉시 표시되는지 확인 |
| 완료 토글 | 체크박스 클릭 → 취소선이 표시되는지 확인 |
| 삭제 | 삭제 버튼 클릭 → 목록에서 즉시 제거되는지 확인 |
| 필터링 | 전체/진행중/완료 탭 클릭 → 해당 항목만 표시되는지 확인 |
| 로컬 저장 | 할일 추가 후 새로고침(`F5`) → 목록이 유지되는지 확인 |
| 다크모드 | 우측 상단 토글 클릭 → 배경/글자색이 전환되는지 확인 |
| 반응형 | 브라우저 개발자도구(F12) → 기기 툴바에서 375px 로 확인 |

### 4. 자동화 테스트 실행

**단위 테스트 (Vitest):**

```bash
npm run test
```

예상 출력 (요약):

```
✓ tests/useTodos.test.ts (5)
✓ tests/TodoItem.test.tsx (3)

Test Files  2 passed (2)
     Tests  8 passed (8)
```

**E2E 테스트 (Playwright):**

```bash
npx playwright test
```

예상 출력 (요약):

```
Running 6 tests using 1 worker

  ✓ 할일을 추가할 수 있다 (1.2s)
  ✓ 할일을 완료 처리할 수 있다 (0.9s)
  ✓ 할일을 삭제할 수 있다 (0.8s)
  ✓ 필터가 정상 동작한다 (1.1s)
  ✓ 새로고침 후에도 목록이 유지된다 (1.4s)
  ✓ 다크모드가 전환된다 (0.7s)

  6 passed (6.1s)
```

> **확인 포인트 ✅**  
> 개발 서버가 정상 실행되고, 단위 테스트와 E2E 테스트가 모두 통과하면 완료.

---

## 버그 수정 (TDD 절차)

현재 `tasks.md` 에 등록된 **"[Bug] Fix: 다크모드 수동 전환 시 배경색 미적용 버그 수정"** 작업을  
TDD(Test-Driven Development) 절차로 진행한다.

### 버그 상황 재현

다크모드 토글 버튼을 클릭했을 때, 텍스트 색상은 바뀌지만 **배경색은 흰색(`#ffffff`)에 머물러 있는 상태**로 가정한다.  
아래 4단계로 이 버그를 고친다.

```
[Test] 실패하는 테스트 작성 (Red)
   ↓
[Verify] 테스트 실행 → 실패 확인
   ↓
[Fix] 코드 수정
   ↓
[Verify] 테스트 실행 → 통과 확인 (Green)
```

---

### Step 1. [Test] 실패하는 테스트 강화

**Copilot Chat** 에 아래를 입력한다.

```
/speckit.clarify
현재 다크모드 E2E 테스트가 텍스트 색상만 확인하고 있어서
배경색 버그를 잡아내지 못하고 있어.

E2E 테스트(tests/e2e/darkmode.spec.ts)에서
다크모드 토글 클릭 후 <html> 또는 <body> 요소의 실제 배경색이
검은색 계열(#0a0a0a)로 변하는지 getComputedStyle 또는
Playwright 의 toHaveCSS 매처로 검증하도록 테스트를 강화해줘.
```

파일 수정 제안이 뜨면 `Accept` 를 클릭한다.

**터미널** 에서 테스트 코드가 강화되었는지 확인한다.

```bash
cat tests/e2e/darkmode.spec.ts
```

예상 출력 (핵심 부분 예시):

```typescript
test('다크모드 전환 시 배경색이 검은색으로 바뀐다', async ({ page }) => {
  await page.goto('http://localhost:3000');
  await page.getByRole('button', { name: '다크모드 전환' }).click();

  const body = page.locator('body');
  await expect(body).toHaveCSS('background-color', 'rgb(10, 10, 10)');
});
```

---

### Step 2. [Verify] 테스트 실행 → 실패 확인 (Red)

**터미널** 에서 실행한다.

```bash
npx playwright test darkmode
```

예상 출력 (버그가 있는 상태이므로 **실패**해야 정상):

```
  ✘ 다크모드 전환 시 배경색이 검은색으로 바뀐다 (1.1s)

  Error: expect(locator).toHaveCSS(expected)

  Expected: rgb(10, 10, 10)
  Received: rgb(255, 255, 255)

  1 failed (1.3s)
```

> **왜 실패를 먼저 확인하는가?**  
> 테스트가 처음부터 통과하면 그 테스트가 버그를 제대로 검증하지 못한다는 뜻이다.  
> 반드시 **빨간불(Red)**을 먼저 확인한 뒤 다음 단계로 넘어간다.

---

### Step 3. [Fix] Tailwind CSS 설정을 고려한 코드 수정

**Copilot Chat** 에 아래를 입력한다.

```
constitution.md 의 [테스트 원칙]에 따라 TDD 로 버그를 수정 중이야.

tests/e2e/darkmode.spec.ts 테스트가 실패하고 있어.
다크모드 토글 시 배경색이 바뀌지 않는 원인을 찾아 고쳐줘.

[확인해줄 부분]
- tailwind.config.ts 의 darkMode 설정이 'class' 로 되어 있는지
- 다크모드 토글 시 <html> 태그에 'dark' 클래스가 실제로 추가/제거되는지
- globals.css 또는 body 태그에 dark:bg-[#0a0a0a] 같은 배경색 클래스가
  실제로 적용되어 있는지
```

Copilot 이 제안하는 수정 사항을 확인하고 `Accept` 를 클릭한다.

**터미널** 에서 수정 내용을 확인한다.

```bash
cat tailwind.config.ts
```

예상 출력 (핵심 부분):

```typescript
const config = {
  darkMode: 'class',
  // ...
};
```

```bash
grep -n "dark" app/globals.css
```

예상 출력 (예시):

```
3:body {
4:  @apply bg-white text-gray-900 dark:bg-[#0a0a0a] dark:text-gray-100;
5:}
```

---

### Step 4. [Verify] 테스트 재실행 → 통과 확인 (Green)

**터미널** 에서 다시 실행한다.

```bash
npx playwright test darkmode
```

예상 출력:

```
  ✓ 다크모드 전환 시 배경색이 검은색으로 바뀐다 (1.0s)

  1 passed (1.2s)
```

전체 테스트도 함께 재실행하여 다른 기능이 깨지지 않았는지 확인한다.

```bash
npm run test && npx playwright test
```

예상 출력 (요약):

```
Test Files  2 passed (2)
     Tests  8 passed (8)

  6 passed (5.9s)
```

브라우저(`http://localhost:3000`)에서 직접 다크모드 토글을 클릭해 배경색이  
검은색으로 바뀌는지 눈으로도 최종 확인한다.

> **확인 포인트 ✅**  
> Playwright 테스트가 Red → Green 으로 전환되고, 브라우저에서 눈으로도 확인되면 완료.

---

## Part 8. 전체 산출물 구조 확인

**터미널** 에서 최종 산출물 구조를 확인한다.

```bash
find .specify/specs -type f
```

예상 출력:

```
.specify/specs/001-todo-list/spec.md
.specify/specs/001-todo-list/plan.md
.specify/specs/001-todo-list/tasks.md
.specify/specs/001-todo-list/checklists/testing.md
```

프로젝트 코드 구조도 확인한다.

```bash
ls app/ components/ hooks/ types/
```

예상 출력 (예시):

```
app/:
  layout.tsx  page.tsx  globals.css

components/:
  TodoInput.tsx  TodoItem.tsx  TodoList.tsx  FilterBar.tsx  DarkModeToggle.tsx

hooks/:
  useTodos.ts  useDarkMode.ts

types/:
  todo.ts
```

> **확인 포인트 ✅**  
> 아래 세 가지가 모두 확인되면 실습 완주다.
> 1. `.specify/specs/001-todo-list/` 아래 `spec.md`, `plan.md`, `tasks.md`, `checklists/testing.md` 가 모두 존재한다
> 2. `npm run test` 와 `npx playwright test` 가 모두 통과한다
> 3. 다크모드 버그가 TDD 절차(Red → Green)로 수정되었다

### 실습 전체 흐름 복습

```
[구현]
constitution(개발원칙 + 테스트원칙)
    → specify(001-todo-list) → plan → tasks → analyze → implement
    → checklist testing(constitution 기반)

[버그 수정, TDD]
clarify(테스트 강화) → 테스트 실행(Red 확인)
    → 코드 수정(Fix) → 테스트 재실행(Green 확인)
```

---

## 트러블슈팅

### Copilot Chat 에서 `/speckit.*` 명령어가 보이지 않는다

VS Code 가 `todolist-project` 폴더를 루트로 열었는지 확인한다.

```bash
pwd
# .../todolist-project 가 출력되어야 한다

code .
```

`.github/prompts/` 폴더가 실제로 존재하는지 확인한다.

```bash
ls .github/prompts/
```

폴더가 없으면 `specify init` 을 다시 실행한다.

```bash
cd ..
specify init todolist-project --integration copilot --force
cd todolist-project
code .
```

---

### Agent 모드가 보이지 않는다

Copilot Chat 패널 하단 드롭다운에서 `Ask / Edit / Agent` 중 `Agent` 를 선택한다.  
`Agent` 항목 자체가 보이지 않으면 VS Code 를 최신 버전으로 업데이트한다.

```
VS Code 버전 확인: Help → About → Version
최소 요구 버전: VS Code 1.90 이상
```

---

### `npm run dev` 실행 시 포트 충돌이 발생한다

이미 3000번 포트를 사용 중인 프로세스가 있는 경우다.

```bash
# macOS / Linux
lsof -i :3000
kill -9 [PID]

# Windows (PowerShell)
netstat -ano | findstr :3000
taskkill /PID [PID] /F
```

또는 다른 포트로 실행한다.

```bash
npm run dev -- -p 3001
```

---

### `npx playwright test` 실행 시 브라우저 오류가 발생한다

Playwright 브라우저 바이너리가 설치되지 않은 경우다.

```bash
npx playwright install
```

설치 완료 후 다시 실행한다.

```bash
npx playwright test
```

---

### `tailwind.config.ts` 에 `darkMode: 'class'` 설정이 없다

Copilot Chat 에서 명시적으로 요청한다.

```
tailwind.config.ts 에 darkMode: 'class' 설정을 추가해줘.
그리고 다크모드 토글 시 <html> 태그에 'dark' 클래스를
추가/제거하는 로직이 hooks/useDarkMode.ts 에 있는지 확인해줘.
```

---

### `/speckit.checklist testing` 결과가 너무 일반적이다 (constitution 원칙이 반영 안 됨)

constitution.md 의 `[테스트 원칙]` 을 명시적으로 언급하여 재실행한다.

```
/speckit.checklist testing
constitution.md 의 [테스트 원칙] 과 spec.md 의 수용 기준을 모두 참조해서
테스트 체크리스트를 다시 만들어줘.
Vitest 단위 테스트와 Playwright E2E 테스트 항목을 각각 구분해서 정리해줘.
```

---

### 채팅 한도 50회가 부족하다

Copilot 에게 직접 아티팩트를 읽어 구현하도록 요청한다.

```
(Copilot Chat 에서)
지금까지 생성된 spec.md, plan.md, tasks.md 파일을 읽고
남은 컴포넌트를 한 번에 구현해줘.
```

또는 채팅 대신 VS Code 의 인라인 코드 완성 기능을 활용한다.  
(인라인 제안은 코드 완성 카운터에서 차감되며 한도가 더 넉넉하다.)

---

### `localStorage` 저장이 동작하지 않는다 (새로고침 시 목록이 사라짐)

`useEffect` 의 의존성 배열 설정 오류인 경우가 많다. Copilot Chat 에서 요청한다.

```
hooks/useTodos.ts 에서 localStorage 저장이 새로고침 후
반영되지 않는 문제가 있어.
- 초기 로딩 시 localStorage 에서 값을 읽어오는 useEffect 와
- todos 상태 변경 시 localStorage 에 저장하는 useEffect 가
  각각 올바른 의존성 배열([]와 [todos])로 분리되어 있는지 확인해줘.
```

---

## 전체 명령어 요약표

| 단계 | 위치 | 명령어 또는 작업 |
|---|---|---|
| uv 설치 (Mac/Linux) | 터미널 | `curl -LsSf https://astral.sh/uv/install.sh \| sh && source ~/.bashrc` |
| uv 설치 (Windows) | PowerShell | `powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 \| iex"` |
| specify 설치 | 터미널 | `uv tool install specify-cli --from git+https://github.com/github/spec-kit.git` |
| 프로젝트 생성 | 터미널 | `specify init todolist-project --integration copilot` |
| 폴더 이동 | 터미널 | `cd todolist-project` |
| VS Code 열기 | 터미널 | `code .` |
| 명령어 파일 확인 | 터미널 | `ls .github/prompts/` |
| Copilot Chat 열기 | VS Code | `Ctrl + Alt + I` |
| Agent 모드 선택 | Copilot Chat | 하단 드롭다운에서 `Agent` 선택 |
| 원칙+테스트원칙 설정 (1회만) | Copilot Chat | `/speckit.constitution` |
| 요구사항 정의 | Copilot Chat | `/speckit.specify` |
| 기술 설계 | Copilot Chat | `/speckit.plan` |
| 작업 목록 | Copilot Chat | `/speckit.tasks` |
| 일관성 점검 | Copilot Chat | `/speckit.analyze` |
| 코드 생성 | Copilot Chat | `/speckit.implement` |
| 구현 후 품질 검증 | Copilot Chat | `/speckit.checklist testing` |
| 의존성 설치 | 터미널 | `npm install` |
| 개발 서버 실행 | 터미널 | `npm run dev` |
| 단위 테스트 실행 | 터미널 | `npm run test` |
| E2E 테스트 실행 | 터미널 | `npx playwright test` |
| 버그 재현 테스트 강화 | Copilot Chat | `/speckit.clarify` |
| 특정 테스트만 실행 | 터미널 | `npx playwright test darkmode` |

**TDD 버그 수정 순서 요약:**

```
[Test]   테스트를 강화하거나 새로 작성한다 (버그를 검증할 수 있도록)
   ↓
[Verify] 테스트를 실행하여 실패(Red)를 확인한다
   ↓
[Fix]    Copilot 에게 원인 분석과 수정을 요청한다
   ↓
[Verify] 테스트를 재실행하여 통과(Green)를 확인한다
   ↓
[Verify] 전체 테스트를 재실행하여 다른 기능이 깨지지 않았는지 확인한다
```

---


## 🎓 학습 포인트

### Spec Driven Development의 핵심

1. **명확성 우선**: 스펙 작성 시 구현 세부사항 배제, 명확화 단계에서 모호성 제거
2. **독립성 보장**: 각 User Story가 독립적으로 테스트 가능, 우선순위별 단계적 구현 가능
3. **TDD 패턴**: Red(테스트 실패) → Green(구현) 반복, 테스트가 구현의 가이드 역할
4. **산출물 연결**: Spec → Plan → Tasks → Implementation, 각 단계의 산출물이 다음 단계의 입력
5. **품질 검증**: Lint/Typecheck로 코드 품질, 테스트로 기능 정확성, 수동 테스트로 UX 확인

### TDD 실천 패턴

1. **테스트 먼저**: 구현 전에 테스트를 작성하여 요구사항을 명확히 함
2. **작은 단위**: 각 작업을 작은 단위로 분해하여 진행
3. **빠른 피드백**: 각 단계마다 테스트 실행으로 즉시 확인
4. **리팩토링 안전망**: 테스트가 있어 리팩토링 시 회귀 방지

---

## 💡 Copilot Chat 활용 팁

1. **단계별 진행**
   ```
   /speckit.specify → /speckit.clarify → /speckit.plan → /speckit.tasks → /speckit.implement
   ```
2. **작업 단위 요청** (한 번에 1-2개씩)
   ```
   T007 작업 진행해줘
   T010 작업 진행해줘
   ```
3. **TDD 주기 명시**
   ```
   T007 테스트 작성 후 Red 확인해줘
   T010 구현 후 Green 확인해줘
   ```
4. **중간 검증**: 각 단계마다 테스트 실행으로 확인
5. **스펙 수정 시**: `clarify → plan → tasks` 순서로 다시 동기화

## 🔧 문제 해결

**"테스트가 통과하지 않아요"**
1. 테스트 파일과 구현 파일을 모두 확인
2. 오류 메시지를 Copilot Chat에 복사하여 해결 요청
3. 필요시 캐시 클리어: `npm run test -- --no-cache`

**"Lint/Typecheck 오류가 발생해요"**
1. 오류 메시지를 확인하고 Copilot Chat에 해결 요청
2. import 누락 확인 / 타입 정의 확인

**"개발 서버가 실행되지 않아요"**
1. 포트 충돌 확인 (3000 포트)
2. 의존성 재설치: `rm -rf node_modules && npm install`

---

## 🔍 심화 학습

### 추가 실습 아이디어
1. **우선순위 변경 기능**: 할일 순서를 드래그앤드롭으로 변경
2. **카테고리 기능**: 할일에 카테고리 태그 추가
3. **검색 기능**: 제목/설명으로 할일 검색
4. **마감일 기능**: 할일에 마감일 추가 및 정렬

### Spec Kit 고급 활용
1. **성능 최적화**: `React.memo`, `useMemo`/`useCallback`, Virtual scrolling
2. **접근성 강화**: 스크린 리더 테스트, 키보드 네비게이션 개선, WCAG 2.1 AA 준수
3. **에러 처리**: ErrorBoundary 추가, Toast 알림 시스템, 오류 로깅

---

## 📚 참고 자료

- [GitHub Spec Kit 문서](https://github.com/github/spec-kit)
- [React 19 Documentation](https://react.dev/)
- [Next.js 16 Documentation](https://nextjs.org/docs)
- [Vitest Documentation](https://vitest.dev/)
- [Playwright Documentation](https://playwright.dev/)
- [WAI-ARIA Authoring Practices](https://www.w3.org/WAI/ARIA/apg/)
