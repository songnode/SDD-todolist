# Feature Specification: 할일 목록 웹앱 (로컬 저장/다크모드/반응형)

**Feature Branch**: `001-todo-list-core`  
**Created**: 2026-08-20  
**Status**: Draft  
**Input**: User description: "할일 추가(제목/설명), 완료 체크/해제, 삭제, 필터(전체/진행중/완료), 로컬 자동 저장, 다크모드, 반응형 디자인"

## Clarifications

### Session 2026-08-20

- Q: 새로 추가한 할일이 목록에서 어디에 보여야 하나요? → A: 새로 추가한 할일이 위(최신 우선)
- Q: 다크모드의 초기값/우선순위 규칙은? → A: 첫 진입은 시스템 선호 모드, 이후 사용자 선택 저장(우선)
- Q: 제목이 비었을 때 입력 검증 UX는? → A: 제목이 비면 “추가” 버튼 비활성화 + 제출 시도 시 안내
- Q: 삭제 전에 확인이 필요한가요? → A: 확인 없이 즉시 삭제(undo 없음)

## User Scenarios & Testing *(mandatory)*

### User Story 1 - 할일을 관리한다 (추가/완료/삭제/필터) (Priority: P1)

사용자는 할일을 입력해 목록에 추가하고, 완료 여부를 토글하며, 필요 없는 할일을 삭제할 수 있습니다. 또한 "전체/진행중/완료" 필터로 원하는 항목만 빠르게 볼 수 있습니다.

**Why this priority**: 핵심 가치(할일 관리)를 제공하는 최소 기능 집합이며, 다른 요구사항(저장/테마/반응형) 없이도 즉시 유용합니다.

**Independent Test**: 브라우저에서 할일 2개를 추가한 뒤, 하나를 완료로 바꾸고, 완료 필터에서 보이는지 확인하며, 삭제 후 목록에서 제거되는지 확인한다.

**Acceptance Scenarios**:

1. **Given** 사용자가 빈 할일 목록 화면을 보고 있을 때, **When** 제목을 입력하고 할일을 추가하면, **Then** 새 할일이 목록 상단에 표시되어야 한다
2. **Given** 사용자가 제목을 비워둔 상태일 때, **When** 할일 추가를 시도하면, **Then** 할일은 추가되지 않아야 하며 "제목을 입력하세요"와 같은 안내가 제공되어야 한다
3. **Given** 사용자가 미완료 할일을 보고 있을 때, **When** 완료 체크를 켜면, **Then** 해당 할일은 완료 상태로 표시되어야 한다
4. **Given** 사용자가 완료된 할일을 보고 있을 때, **When** 완료 체크를 끄면, **Then** 해당 할일은 미완료 상태로 표시되어야 한다
5. **Given** 사용자가 할일이 있는 목록을 보고 있을 때, **When** 특정 할일을 삭제하면, **Then** 확인 단계 없이 즉시 삭제되어 목록에서 제거되어야 한다
6. **Given** 완료/미완료가 섞인 목록이 있을 때, **When** 필터를 "진행중"으로 변경하면, **Then** 미완료 할일만 표시되어야 한다

---

### User Story 2 - 새로고침해도 할일이 유지된다 (자동 저장/복원) (Priority: P2)

사용자는 페이지를 새로고침하거나 브라우저를 닫았다가 다시 열어도 이전에 작성한 할일 목록(제목/설명/완료 상태/필터 상태 등)이 유지되기를 기대합니다.

**Why this priority**: 할일 관리 앱의 신뢰성과 반복 사용성을 크게 높입니다.

**Independent Test**: 할일을 1개 이상 추가하고 완료 상태를 변경한 뒤 페이지를 새로고침하여 동일 상태로 복원되는지 확인한다.

**Acceptance Scenarios**:

1. **Given** 사용자가 할일을 추가/변경한 상태일 때, **When** 페이지를 새로고침하면, **Then** 할일 목록/완료 상태/필터/테마가 이전과 동일하게 복원되어야 한다
2. **Given** 저장된 데이터가 손상되었거나 읽을 수 없을 때, **When** 앱을 열면, **Then** 앱은 중단되지 않고 빈 상태로 안전하게 시작하며 사용자에게 안내를 제공해야 한다

---

### User Story 3 - 환경에 맞는 화면을 제공한다 (다크모드/반응형) (Priority: P3)

사용자는 밝은/어두운 환경에 맞는 테마를 사용할 수 있고, 모바일과 데스크톱에서 모두 무리 없이 사용할 수 있어야 합니다.

**Why this priority**: 사용 편의성과 접근성을 개선하고, 다양한 디바이스에서의 사용을 가능하게 합니다.

**Independent Test**: 다크모드를 켜고 끈 뒤 화면 대비/가독성을 확인하고, 모바일 폭에서도 주요 기능(추가/필터/완료/삭제)을 수행할 수 있는지 확인한다.

**Acceptance Scenarios**:

1. **Given** 사용자가 다크모드 설정을 변경할 수 있을 때, **When** 다크모드를 켜면, **Then** 화면은 어두운 테마로 전환되어야 하고 텍스트/컨트롤은 충분한 대비를 유지해야 한다
2. **Given** 사용자가 모바일 화면에서 앱을 사용할 때, **When** 할일을 추가/완료/삭제/필터링을 수행하면, **Then** 모든 조작이 화면 깨짐 없이 가능해야 한다
3. **Given** 사용자가 처음 앱에 진입했고 저장된 테마 설정이 없을 때, **When** 화면이 초기 렌더링되면, **Then** 시스템 선호(라이트/다크)를 기본값으로 따라야 한다
4. **Given** 사용자가 테마를 명시적으로 선택해 저장한 적이 있을 때, **When** 앱을 다시 열면, **Then** 시스템 선호와 무관하게 저장된 테마를 우선 적용해야 한다

### Edge Cases

- 사용자가 제목을 비워둔 채 할일을 추가하려고 하면, 추가가 되지 않고(버튼 비활성화), 시도 시 안내가 제공되는가?
- 제목/설명이 매우 길거나 줄바꿈이 포함되면 표시가 무너지지 않는가?
- 할일이 0개일 때 필터를 바꾸면 어떤 빈 상태가 보이는가?
- 저장 공간이 제한되거나 로컬 저장이 불가능한 환경에서 앱이 어떻게 동작하는가?
- 저장된 데이터 스키마가 변경되었을 때(버전 차이) 안전하게 처리되는가?

## Requirements *(mandatory)*

### Functional Requirements

- **FR-001**: 사용자는 제목과 설명(설명은 선택)을 입력하여 할일을 추가할 수 있어야 한다
- **FR-002**: 시스템은 제목이 비어 있거나 공백만 있는 할일의 추가를 허용하지 않아야 한다
- **FR-003**: 사용자는 할일의 완료 상태를 체크/해제로 전환할 수 있어야 한다
- **FR-004**: 사용자는 할일을 삭제할 수 있어야 한다
- **FR-005**: 사용자는 "전체 / 진행중 / 완료" 3가지 상태로 목록을 필터링할 수 있어야 한다
- **FR-006**: 시스템은 사용자의 변경(추가/완료 토글/삭제/필터/테마)을 로컬에 자동 저장해야 한다
- **FR-007**: 시스템은 앱 시작 시 로컬에 저장된 할일 목록과 설정을 자동으로 복원해야 한다
- **FR-008**: 시스템은 로컬 데이터가 손상되었거나 읽을 수 없는 경우에도 앱이 중단되지 않아야 하며, 안전한 초기 상태로 시작하고 사용자에게 안내해야 한다
- **FR-009**: 사용자는 라이트/다크 테마를 사용할 수 있어야 한다
- **FR-010**: 시스템은 모바일과 데스크톱 화면 크기에서 주요 기능이 손상 없이 동작하도록 레이아웃을 제공해야 한다
- **FR-011**: 시스템은 새로 추가한 할일이 목록 상단에 표시되도록 정렬해야 한다
- **FR-012**: 시스템은 저장된 테마 설정이 없으면 시스템 선호(라이트/다크)를 기본값으로 사용해야 하며, 사용자가 테마를 선택하면 이를 저장하고 이후에는 저장된 값을 우선 적용해야 한다
- **FR-013**: 시스템은 제목이 비어 있거나 공백만 있는 경우 "추가" 동작을 비활성화해야 하며, 사용자가 추가를 시도하면(예: Enter 키 등) 제목 입력 안내를 제공해야 한다
- **FR-014**: 시스템은 사용자가 삭제를 수행하면 확인 단계 없이 즉시 삭제해야 하며, 삭제 취소(undo)는 제공하지 않아야 한다

### Assumptions

- 이 앱은 단일 사용자(개인) 기준이며 로그인/공유/동기화는 포함하지 않는다
- 데이터는 사용자의 브라우저/디바이스에 로컬로 저장된다
- 네트워크 연결이 없더라도 기본 기능(추가/완료/삭제/필터/저장/테마)은 동작해야 한다

### Out of Scope

- 사용자 계정, 다중 디바이스 동기화, 서버 저장소
- 할일의 마감일/우선순위/태그/검색
- 삭제 취소(undo) 또는 휴지통(기능 제공하지 않음)

### Key Entities *(include if feature involves data)*

- **TodoItem**: 사용자가 관리하는 단일 할일
  - 핵심 속성: 고유 식별자, 제목, 설명(선택), 완료 여부, 생성 시각(선택), 마지막 수정 시각(선택)
- **UserPreferences**: 사용자 환경 설정
  - 핵심 속성: 선택된 필터, 테마(라이트/다크)

## Success Criteria *(mandatory)*

### Measurable Outcomes

- **SC-001**: 신규 사용자는 최초 진입 후 60초 이내에 할일 1개를 추가하고 완료 체크를 할 수 있다
- **SC-002**: 사용자가 페이지를 새로고침해도 직전 상태(목록/완료/필터/테마)가 99% 이상 동일하게 유지된다
- **SC-003**: 할일 500개 기준에서 필터 변경 후 0.2초 이내에 결과가 화면에 반영된다
- **SC-004**: 모바일(가로 360px 수준)과 데스크톱(가로 1280px 수준)에서 핵심 작업(추가/완료/삭제/필터)이 1회 이상 성공적으로 수행된다
