# 01_01.Kiro.dev Specs vs SpecKit

---

## 1. 학습 목표

- AI 코딩 시대에 "속도"보다 "올바른 것을 만드는가"가 왜 더 중요한 병목이 되었는지 설명할 수 있다
- Kiro.dev Specs와 SpecKit의 워크플로우 차이, 강점/약점을 실무 기준으로 비교할 수 있다
- "이분법적 선택"이 아니라 두 도구를 함께 쓰는 하이브리드 전략을 이해하고 자신의 프로젝트에 적용할 수 있다

---

## 2. 문제 제기: 속도는 더 이상 병목이 아니다

원문의 핵심 문제의식은 다음과 같습니다.

> AI 코딩 도구는 점점 빨라지고 있다. 하지만 속도는 더 이상 핵심 병목이 아니다. 진짜 문제는 팀이 **더 명확한 요구사항, 더 탄탄한 계획, 더 적은 실수, 더 나은 장기 유지보수성**을 갖고 "올바른 것"을 만들 수 있느냐다.

명세(spec) 없이 AI가 빠르게 코드를 뽑아내면 다음과 같은 문제가 누적됩니다.

- 불명확한 요구사항
- 누락된 예외 케이스(edge case)
- 아키텍처가 서서히 어긋나는 현상(architectural drift)
- 일관성 없는 구현
- 약한 테스트 커버리지
- 같은 결정을 중복해서 내리는 비효율
- 겉으로는 드러나지 않는 기술 부채
- 지금은 동작하지만 나중에 유지보수하기 어려운 코드

→ 그래서 코드가 아니라 **의도(intent)**에서 출발해, 스펙을 진실의 원천으로 삼고 코드·테스트·계획·작업이 모두 그로부터 파생되어야 한다는 것이 SDD의 전제입니다.

---

## 3. Kiro.dev Specs: 속도와 가이드된 실행에 강함

Kiro Specs는 Kiro AI IDE에 **내장된** 기능입니다. 워크플로우는 단순합니다.

```
Requirements → Design → Tasks → Execution
```

### 강점
- IDE 안에서 요구사항·설계·작업 계획·실행이 긴밀하게 연결되어, 별도 프로세스를 관리할 필요가 없음
- 빠르게 시작 가능, 따라가기 쉬움
- 개발자 경험(DX)이 뛰어남
- 명확한 작업 실행 흐름
- 1인 개발자·소규모 팀에 특히 유용
- 막연한 아이디어를 구조화된 작업으로 빠르게 전환

### 약점 (핵심은 "이식성")
- 하나의 생태계(Kiro)에 대한 의존도가 높아짐
- 여러 AI 코딩 도구를 넘나드는 이식성이 떨어짐
- 완전한 SDD 프레임워크에 비해 산출물(artefact)의 깊이가 얕음
- 거버넌스가 중요한 환경에는 다소 부족
- 규모가 커지면 크레딧 기반 과금이 변수로 작용

**요약**: Kiro Specs는 "실행 속도"에는 탁월하지만, 대규모 조직의 유일한 장기 엔지니어링 표준으로 쓰기에는 무리가 있습니다.

---

## 4. SpecKit: 장기적 구조와 확장성에 강함

SpecKit은 IDE의 부가기능이 아니라, **영속적인 프로젝트 산출물**을 중심으로 설계된 독립적인 SDD 프레임워크입니다.

```
Constitution → Specify → Clarify → Plan → Tasks → Analyze → Implement
```

Kiro의 단순한 "요구사항→작업" 흐름보다 훨씬 깊은 프로세스입니다.

### 강점
- 리포지토리 중심(repo-first) 워크플로우
- 에이전트 비종속적(agent-agnostic) — 어떤 AI 도구를 쓰든 무방
- 강한 거버넌스
- 더 풍부한 계획 산출물
- 요구사항→구현까지의 추적성(traceability)이 뛰어남
- 대규모 팀에 적합
- 복잡한 시스템에 적합
- 특정 IDE나 AI 도구 하나에 종속되지 않고 더 오래 지속됨

### 약점
- 셋업이 더 많이 필요
- 프로세스 오버헤드가 큼
- 학습 곡선이 가파름
- 작은 기능 하나 추가하는 데는 과하게 느껴질 수 있음
- 실행 경험은 결국 선택한 AI 에이전트/개발 환경에 따라 달라짐

**요약**: 작은 작업에는 SpecKit이 과할 수 있습니다. 모든 변경이 풀(full) 스펙 워크플로우를 필요로 하는 것은 아닙니다.

---

## 5. 실무 비교표

| 질문 | 더 나은 선택 | 이유 |
|---|---|---|
| 속도가 최우선인가? | Kiro Specs | 통합적이고, 가이드가 있고, 시작하기 쉬움 |
| 장기적으로 쓸 것인가? | SpecKit | 산출물이 도구 하나에 종속되지 않고 오래 지속됨 |
| 대규모 프로젝트인가? | SpecKit | 대규모 시스템은 더 강한 계획·추적성이 필요함 |
| 프로세스 실수를 줄이는 게 목표인가? | SpecKit | 구현 전에 더 많은 체크포인트를 만듦 |
| 일상적인 워크플로우 마찰을 줄이는 게 목표인가? | Kiro Specs | 하나의 IDE 경험 안에 모든 작업이 머무름 |
| 1인 개발자인가? | Kiro Specs | 오버헤드가 낮고 실행이 빠름 |
| 여러 도구를 쓰는 팀인가? | SpecKit | 더 이식성이 높고 에이전트 비종속적 |
| 거버넌스가 중요한 업무인가? | SpecKit | 명세·계획·리뷰 구조가 더 탄탄함 |

---

## 6. 저자의 결론: 이분법이 아니라 조합

원문 저자는 "Kiro냐 SpecKit이냐"라는 양자택일 프레임 자체를 거부합니다. 대신 다음과 같은 조합을 제안합니다.

> **SpecKit을 장기적인 엔지니어링 표준(durable process)으로 쓰고, Kiro.dev를 속도가 중요할 때의 실행 환경(execution environment) 중 하나로 쓴다.**

구체적으로:

- **SpecKit으로 정의할 것**: 원칙(principles), 명세(specifications), 계획(plans), 작업(tasks), 계약(contracts), 체크리스트(checklists), 검증 단계(validation steps), 거버넌스 기대치
- **Kiro.dev를 쓸 때**: 팀이 더 빠르고 매끄러운, IDE 네이티브 구현 경험을 원할 때

이렇게 하면 "하나의 도구에만 의존하는 리스크"를 피하면서도, Kiro의 뛰어난 개발자 경험을 그대로 활용할 수 있습니다.

---

## 7. 언제 무엇을 쓸까 — 실전 가이드

### Kiro.dev Specs를 쓸 때
- 속도가 중요할 때
- 팀이 작을 때
- 기능이 그리 복잡하지 않을 때
- 가이드된 워크플로우를 원할 때
- 하나의 AI IDE 안에서 작업하고 싶을 때
- 아이디어에서 구현까지 빠르게 이동해야 할 때
- 격식(ceremony)은 줄이고 흐름(flow)을 원할 때

### SpecKit을 쓸 때
- 프로젝트가 클 때
- 시스템이 수년간 운영될 예정일 때
- 여러 개발자가 참여할 때
- 요구사항의 추적성이 필요할 때
- 거버넌스가 중요할 때
- 여러 도구·AI 에이전트를 함께 쓸 때
- 아키텍처 결정을 문서화해야 할 때
- 실수를 나중에 고치는 비용이 클 때

---

## 8. 실수의 종류가 다르다

원문에서 흥미로운 지점: "어느 쪽이 실수를 더 줄이는가"는 **실수의 종류**에 따라 답이 달라집니다.

| 실수 유형 | 더 잘 막는 도구 | 이유 |
|---|---|---|
| 워크플로우 실수 (요구사항→설계→작업 흐름이 끊기는 것) | Kiro.dev | 하나의 환경 안에서 깔끔하게 이어짐 |
| 프로세스/계획 실수 (모호함, 요구사항 누락, 아키텍처 drift) | SpecKit | 구현 전 더 많은 사고(thinking)를 강제함 |

→ 소규모 팀에는 Kiro가 "더 안전하게" 느껴질 수 있고, 대규모·복잡 시스템에는 SpecKit이 "더 안전"합니다. 도구의 우열이 아니라 **상황에 맞는 규율의 정도**가 다른 것입니다.

---

## 9. 참고 자료

- DXBMARK LLC, "Kiro Specs vs SpecKit", LinkedIn Pulse (2026.05.19)
