# GitHub Spec Kit 으로 할일 목록 웹앱 만들기 실습
## 주제: 할일 목록(Todo List) 웹앱 만들기 (GitHub Copilot Free + VS Code)

> **이 실습은 Next.js 기반의 할일 목록 웹앱을 만드는 실습이다.**  
> **도구는 GitHub Copilot Free 를 사용**하여 SDD(Spec-Driven Development) 흐름을  
> 요구사항 정의 → 설계 → 작업 분해 → 구현 → 버그 수정(TDD) 순서로 직접 실습한다.

**목표**: VS Code 환경에서 SDD 전체 흐름을 Copilot 으로 체험하고, 할일 목록 웹앱을 완성한다  
**사용 도구**: VS Code (로컬), GitHub Copilot Free, specify CLI, Node.js  
**결과물**: Next.js + TypeScript + Tailwind CSS 기반 할일 목록 웹앱 (추가/완료/삭제/필터링/다크모드/로컬저장)

---

## 목차

1. [사전 확인 — Copilot Free 무료 한도](#사전-확인--copilot-free-무료-한도)
2. [사전 확인 — Copilot 연동 시 파일 위치](#사전-확인--copilot-연동-시-파일-위치)
3. [실습 전 준비 사항](#실습-전-준비-사항)
4. [Part 1. VS Code 환경 준비](#part-1-vs-code-환경-준비)
5. [Part 2. specify CLI 설치](#part-2-specify-cli-설치)
6. [Part 3. 프로젝트 초기화](#part-3-프로젝트-초기화)
7. [Part 4. Copilot 채팅창 설정](#part-4-copilot-채팅창-설정)
8. [Part 5. 할일 목록 웹앱 구현 실습](#part-5-할일-목록-웹앱-구현-실습)
   - Step 1. Constitution — 프로젝트 원칙 설정
   - Step 2. Specify — 요구사항 정의
   - Step 3. Plan — 기술 스택 및 설계
   - Step 4. Tasks — 작업 목록 생성
   - Step 5. Analyze — 일관성 점검
   - Step 6. Implement — 코드 생성
   - Step 7. Checklist — 구현 품질 검증 (implement 후)
9. [Part 6. 결과물 실행 및 테스트](#part-6-결과물-실행-및-테스트)
10. [Part 7. 버그 수정 작업 실습 (TDD 절차)](#part-7-버그-수정-작업-실습-tdd-절차)
    - Step 1. [Test] 실패하는 테스트 작성 (Red)
    - Step 2. [Verify] 테스트 실패 확인
    - Step 3. [Fix] 코드 수정 (Green)
    - Step 4. [Refactor] 리팩터링 및 재검증
11. [Part 8. 전체 산출물 구조 확인](#part-8-전체-산출물-구조-확인)
12. [트러블슈팅](#트러블슈팅)
13. [전체 명령어 요약표](#전체-명령어-요약표)

---

## 사전 확인 — Copilot Free 무료 한도

GitHub Copilot Free 는 GitHub 계정만 있으면 별도 결제 없이 바로 사용 가능하다.  
단, 아래 월간 사용 한도가 있으므로 실습 중 신중하게 진행해야 한다.

| 기능 | 월간 무료 한도 | 이번 실습 예상 사용량 |
|---|---|---|
| 코드 완성 (인라인 제안) | 2,000회 | 약 300~500회 (컴포넌트·훅·테스트 코드 자동완성) |
| 채팅 메시지 | 50회 | 약 20~25회 (SDD 7단계 + 버그 수정 4단계 + 점검) |

> **채팅 한도 절약 수칙**
>
> - 각 단계 입력 전 내용을 충분히 검토하고 한 번에 정확하게 입력한다
> - 오류 발생 시 전체 재요청 대신 해당 부분만 수정 요청한다
> - 채팅창에서 잡담성 질문은 삼간다
> - 실습 외 다른 프로젝트에서 Copilot 사용을 줄인다

---

## 사전 확인 — Copilot 연동 시 파일 위치

`specify init` 실행 시 에이전트별로 생성되는 폴더 경로가 다르다.  
이 실습은 **GitHub Copilot** 을 사용하므로 아래 경로에 명령어 파일이 생성된다.

| 에이전트 | 명령어 파일 위치 | 파일 형식 |
|---|---|---|
| GitHub Copilot | `.github/prompts/` | `.prompt.md` |
| Gemini CLI | `.gemini/commands/` | `.toml` |
| Claude Code | `.claude/commands/` | `.md` |

> **Copilot의 경우** `.github/prompts/` 경로가 **올바른 경로**이다.  
> VS Code 는 `.github/prompts/*.prompt.md` 파일을 자동으로 인식하여  
> Copilot 채팅창에서 `/speckit.*` 명령어로 노출한다.

---

## 실습 전 준비 사항

| 필요 항목 | 확인/설치 주소 | 비고 |
|---|---|---|
| GitHub 계정 | https://github.com/signup | Copilot Free 인증에 사용 |
| Node.js (LTS, 18 이상 권장) | https://nodejs.org | Next.js 실행에 필요 |
| VS Code | https://code.visualstudio.com | 로컬 개발 환경 |

> Google 계정(Gemini)이나 Anthropic 계정(Claude)은 필요하지 않다.  
> GitHub 계정과 Node.js 설치만으로 실습을 시작할 수 있다.

**브라우저**: Chrome 또는 Edge 권장

**Node.js 설치 확인:**

```bash
node --version
npm --version
```

예상 출력 (버전 번호는 다를 수 있음):

```
v20.11.0
10.2.4
```

> 두 명령어 모두 버전이 출력되지 않으면 Node.js 를 먼저 설치한 뒤 다음 단계로 진행한다.

---

## Part 1. VS Code 환경 준비

### 1-1. 작업 폴더 생성

```bash
# Windows (PowerShell)
mkdir C:\Users\%USERNAME%\todolist-copilot-lab
cd C:\Users\%USERNAME%\todolist-copilot-lab

# macOS / Linux
mkdir ~/todolist-copilot-lab
cd ~/todolist-copilot-lab
```

**현재 위치 확인:**

```bash
pwd
```

예상 출력 (macOS/Linux 기준):

```
/Users/사용자명/todolist-copilot-lab
```

### 1-2. GitHub Copilot 활성화

1. VS Code 우측 하단 계정 아이콘을 클릭한다.
2. `Sign in with GitHub` 를 선택한다.
3. 브라우저에서 GitHub 계정으로 로그인한다.
4. VS Code 하단 상태바에서 Copilot 아이콘이 활성화(흰색)되면 정상이다.

> Copilot 아이콘에 X 표시가 있으면 로그인이 필요한 상태이다.

> **확인 포인트 ✅**  
> 작업 폴더가 생성되고, Copilot 아이콘이 활성화되면 Part 1 완료.

---

## Part 2. specify CLI 설치

Copilot 은 VS Code 에 내장되어 있으므로 별도 CLI 설치가 필요 없다.  
`specify CLI` 만 설치하면 된다.

### 2-1. uv 설치 (Python 패키지 관리자)

**macOS / Linux:**

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
source ~/.bashrc
```

**Windows (PowerShell):**

```powershell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

> Windows 에서 설치 후 새 터미널 창을 열어야 `uv` 명령이 인식된다.

설치 확인:

```bash
uv --version
```

예상 출력:

```
uv 0.x.x
```

### 2-2. specify CLI 설치

```bash
uv tool install specify-cli --from git+https://github.com/github/spec-kit.git
```

설치 확인:

```bash
specify --version
```

예상 출력:

```
specify 0.x.x
```

> **확인 포인트 ✅**  
> `uv` 와 `specify` 버전이 모두 출력되면 Part 2 완료.

---

## Part 3. 프로젝트 초기화

### 3-1. specify 프로젝트 생성

`todolist-copilot-lab` 폴더 안에서 아래를 실행한다.

```bash
specify init todolist-project --integration copilot
```

실행 중 에이전트 선택 화면이 나오면 `copilot` 을 선택한다.

### 3-2. 프로젝트 폴더로 이동

```bash
cd todolist-project
pwd
```

예상 출력:

```
/Users/사용자명/todolist-copilot-lab/todolist-project
```

> **중요**: 이후 모든 터미널 명령어는 반드시 `todolist-project` 폴더 안에서 실행해야 한다.

### 3-3. 생성된 구조 확인

```bash
ls -la
```

예상 출력:

```
.github/
.specify/
README.md
```

Copilot 명령어 파일 확인:

```bash
ls .github/prompts/
```

아래 파일들이 모두 보이면 정상이다.

```
speckit.analyze.prompt.md
speckit.checklist.prompt.md
speckit.clarify.prompt.md
speckit.constitution.prompt.md
speckit.implement.prompt.md
speckit.plan.prompt.md
speckit.specify.prompt.md
speckit.tasks.prompt.md
```

`.specify` 내부도 확인한다.

```bash
ls .specify/
```

예상 출력:

```
memory/   specs/   templates/
```

### 3-4. VS Code 에서 프로젝트 폴더 열기 (중요)

`/speckit.*` slash 명령어가 동작하려면 VS Code 가 `todolist-project` 폴더를 **루트**로 인식해야 한다.

```bash
code .
```

또는 VS Code 상단 메뉴에서 `File → Open Folder` → `todolist-project` 폴더를 선택한다.

VS Code 가 `todolist-project` 를 루트로 열면 좌측 탐색기에 `.github/`, `.specify/` 폴더가 보인다.

> **확인 포인트 ✅**  
> 좌측 탐색기에 `.github/` 와 `.specify/` 가 보이면 Part 3 완료.

---

## Part 4. Copilot 채팅창 설정

### 4-1. Copilot Chat 패널 열기

아래 방법 중 하나로 Copilot Chat 을 연다.

| 방법 | 단축키 / 경로 |
|---|---|
| 키보드 단축키 (Windows/Linux) | `Ctrl + Alt + I` |
| 키보드 단축키 (Mac) | `Cmd + Option + I` |
| 상단 메뉴 | `View → GitHub Copilot Chat` |
| VS Code 우측 상단 아이콘 | Copilot 아이콘 클릭 → `Open Chat` |

### 4-2. Agent 모드 확인

Copilot Chat 패널 하단 입력창 옆 드롭다운에서 모드를 확인한다.

```
[ Ask ]  [ Edit ]  [ Agent ]
```

반드시 `Agent` 를 선택해야 한다.

> **Agent 모드를 선택해야 하는 이유**  
> `Ask` 모드: 질문과 답변만 가능, 파일 생성 불가  
> `Edit` 모드: 선택한 파일만 수정 가능  
> `Agent` 모드: 파일 생성·수정·명령 실행 모두 가능 → **SDD 실습에 필수**

### 4-3. slash 명령어 확인

Copilot Chat 입력창에 `/` 를 입력하면 사용 가능한 명령어 목록이 나타난다.

```
/speckit.constitution
/speckit.specify
/speckit.clarify
/speckit.checklist
/speckit.plan
/speckit.tasks
/speckit.analyze
/speckit.implement
```

위 목록이 보이면 준비가 완료된 것이다.  
목록이 보이지 않으면 [트러블슈팅 — Copilot Chat 에서 명령어가 보이지 않는다](#copilot-chat-에서-speckit-명령어가-보이지-않는다) 를 확인한다.

> **확인 포인트 ✅**  
> `/speckit.*` 명령어 목록이 보이면 Part 4 완료. 실습 시작 가능.

---

## Part 5. 할일 목록 웹앱 구현 실습

이제부터 모든 `/speckit.*` 명령어는 터미널이 아닌 **Copilot Chat 패널** 에 입력한다.  
Copilot 이 파일 생성 또는 수정을 제안하면 반드시 **`Accept`** 버튼을 클릭하여 적용한다.

### 실습 목표

Next.js + TypeScript + Tailwind CSS 로 구성된 할일 목록 웹앱을 아래 7단계를 거쳐 만든다.

```
constitution → specify → plan → tasks → analyze → implement → checklist(testing)
```

---

### Step 1. Constitution — 프로젝트 원칙 설정

> **[개발/테스트 원칙]** — `constitution.md` 는 프로젝트 전체에 적용되는 규칙이며,  
> Copilot 이 이후 모든 단계에서 항상 참조하는 배경 지식이다. **1회만 실행**한다.

**Copilot Chat** 에 아래를 입력한다.

```
/speckit.constitution
이 프로젝트는 할일 목록(Todo List) 웹앱입니다.

[개발 원칙]
- Next.js(App Router) + React + TypeScript 로 구성한다
- 스타일링은 Tailwind CSS 만 사용한다
- 컴포넌트는 기능 단위로 분리한다 (TodoInput, TodoItem, TodoList, FilterBar 등)
- 코드에 한국어 주석을 달아 초심자가 이해할 수 있도록 한다
- 모바일과 데스크탑 모두에서 보기 좋게 반응형으로 만든다
- 불필요하게 복잡한 상태 관리 라이브러리는 사용하지 않는다 (React Hooks 만 사용)
- 모든 로그 및 AI 와의 대화는 한국어로 한다

[테스트 원칙]
- 단위 테스트는 Vitest + React Testing Library 로 작성한다
- E2E 테스트는 Playwright 로 작성한다
- implement 완료 후 반드시 /speckit.checklist testing 으로 품질을 검증한다
- 각 수용 기준(Given-When-Then)에 대응하는 테스트 케이스를 1:1 로 작성한다
- 버그 수정 시에는 테스트를 먼저 작성해 실패를 확인한 뒤(Red) 코드를 수정한다(Green)
- 모바일 화면(375px 기준)에서도 동작을 확인한다
```

Copilot 이 파일 생성을 제안하면 `Accept` 를 클릭한다.

**터미널** 에서 저장 여부를 확인한다.

```bash
cat .specify/memory/constitution.md
```

> **확인 포인트 ✅**  
> `constitution.md` 에 `[개발 원칙]` 과 `[테스트 원칙]` 섹션이 **모두** 포함되어 있는가?  
> 이후 `/speckit.checklist testing` 실행 시 이 원칙이 체크리스트에 자동 반영된다.

---

### Step 2. Specify — 요구사항 정의

> **[무엇을, 왜 만드는가]** — 기술 구현 방법은 적지 않는다. 사용자 관점의 요구사항만 정의한다.

**Copilot Chat** 에 아래를 입력한다.

```
/speckit.specify
다음 기능을 가진 할일 목록 웹앱을 만듭니다.

[기능 요구사항]
1. 할일 추가 — 제목(필수)과 설명(선택)을 입력하여 새 할일을 추가한다
2. 할일 완료 체크/해제 — 체크박스를 눌러 완료 상태를 토글한다
3. 할일 삭제 — 삭제 버튼을 눌러 목록에서 제거한다 (삭제 전 확인 없음)
4. 필터링 — 전체 / 진행중 / 완료 세 가지 탭으로 목록을 필터링한다
5. 로컬 스토리지 자동 저장 — 새로고침해도 목록이 유지된다
6. 다크모드 지원 — 우측 상단 토글 버튼으로 라이트/다크 모드를 전환한다
7. 반응형 디자인 — 모바일(375px)과 데스크탑(1280px) 모두에서 자연스럽게 보인다

[수용 기준 예시]
- 제목 없이 추가 버튼을 누르면 "제목을 입력하세요" 메시지가 표시된다
- 할일이 하나도 없으면 "할일이 없습니다" 안내 문구가 표시된다
- 완료된 할일은 제목에 취소선이 표시된다
```

파일 생성 제안이 뜨면 `Accept` 를 클릭한다.

**터미널** 에서 확인한다.

```bash
ls .specify/specs/
# 예상 출력: 001-todo-list/

cat .specify/specs/001-todo-list/spec.md
```

> **확인 포인트 ✅**  
> `spec.md` 에 7개 기능 요구사항과 수용 기준이 모두 반영되어 있으면 Step 3 으로 진행.

---

### Step 3. Plan — 기술 스택 및 설계

> **[어떻게 만들 것인가]** — `constitution.md` 의 원칙(Next.js, Tailwind, Hooks 만 사용)을  
> Copilot 이 자동으로 반영하여 기술 설계를 만든다.

**Copilot Chat** 에 아래를 입력한다.

```
/speckit.plan
[기술 스택]
- Next.js (App Router)
- React
- TypeScript
- Tailwind CSS
- React Hooks (useState, useEffect, useContext) 로 상태 관리
- Browser Local Storage 로 데이터 저장
- Vitest + React Testing Library (단위 테스트)
- Playwright (E2E 테스트)
- ESLint + Prettier

[UI/UX]
- Tailwind CSS 를 사용한 모던하고 깔끔한 UI/UX
- 카드 형태의 할일 아이템, 여백을 넉넉하게 사용
- 다크모드는 Tailwind 의 dark: 클래스와 class 전략을 사용한다
```

파일 생성 제안이 뜨면 `Accept` 를 클릭한다.

**터미널** 에서 확인한다.

```bash
cat .specify/specs/001-todo-list/plan.md
```

> **확인 포인트 ✅**  
> `plan.md` 에 폴더 구조, 컴포넌트 목록, 상태 관리 방식이 구체적으로 나와 있으면 Step 4 로 진행.

---

### Step 4. Tasks — 작업 목록 생성

> **[즉시 실행 가능한 작업 단위로 분해]** — `plan.md` 를 바탕으로 구현 순서를 정한다.

**Copilot Chat** 에 아래를 입력한다.

```
/speckit.tasks
```

파일 생성 제안이 뜨면 `Accept` 를 클릭한다.

**터미널** 에서 확인한다.

```bash
cat .specify/specs/001-todo-list/tasks.md
```

예상 출력 (요약):

```
[ ] Task 1: Next.js 프로젝트 스캐폴딩 (create-next-app, Tailwind 설정)
[ ] Task 2: Todo 타입 정의 (types/todo.ts)
[ ] Task 3: useTodos 커스텀 훅 작성 (추가/토글/삭제 로직)
[ ] Task 4: localStorage 연동 (useEffect 로 저장/불러오기)
[ ] Task 5: TodoInput 컴포넌트 작성
[ ] Task 6: TodoItem, TodoList 컴포넌트 작성
[ ] Task 7: FilterBar 컴포넌트 작성 (전체/진행중/완료)
[ ] Task 8: 다크모드 토글 기능 작성
[ ] Task 9: 반응형 레이아웃 스타일링
[ ] Task 10: Vitest 단위 테스트 작성
[ ] Task 11: Playwright E2E 테스트 작성
```

---

### Step 5. Analyze — 일관성 점검

> **[검증]** — `spec.md`, `plan.md`, `tasks.md`, `constitution.md` 간 불일치를 자동으로 탐지한다.

**Copilot Chat** 에 아래를 입력한다.

```
/speckit.analyze
```

"이상 없음" 또는 "검토 완료" 메시지가 나오면 다음 단계로 진행한다.  
불일치가 발견되면 Copilot 이 안내하는 항목을 수정한 후 `/speckit.analyze` 를 재실행한다.

---

### Step 6. Implement — 코드 생성

> **[사양을 코드로 변환]** — `spec.md` 의 수용 기준이 코드에 1:1 로 반영되는지 이후 테스트에서 확인한다.

**Copilot Chat** 에 아래를 입력한다.

```
/speckit.implement
```

Copilot 이 여러 파일 생성을 순차적으로 제안한다. 각 제안마다 `Accept` 를 클릭하거나 `Accept All` 을 클릭한다.

> 프로젝트 스캐폴딩(`create-next-app`) 단계에서 터미널에 실행 확인 프롬프트가 뜨면  
> Copilot Chat 하단에 나타나는 `Run` / `실행` 버튼을 클릭하여 진행을 허용해야 한다.

생성 완료 후 **터미널** 에서 확인한다.

```bash
ls -la
```

예상 출력 (일부):

```
app/
components/
hooks/
types/
tests/
package.json
tailwind.config.ts
```

의존성 설치 여부 확인:

```bash
cat package.json
```

`next`, `react`, `tailwindcss`, `vitest`, `@playwright/test` 항목이 보이면 정상이다.

---

### Step 7. Checklist — 구현 품질 검증 (implement 후)

> **[품질 게이트]** — `implement` 완료 후 생성된 코드가 `spec.md` 의 모든 수용 기준을 충족하는지  
> `constitution.md` 의 `[테스트 원칙]` 을 기준으로 점검한다.

**Copilot Chat** 에 아래를 입력한다.

```
/speckit.checklist testing
생성된 코드가 spec.md 의 7가지 기능 요구사항과 수용 기준을 모두 충족하는지
테스트 시나리오 관점에서 점검해줘.
constitution.md 의 [테스트 원칙] 도 함께 반영해서 체크리스트를 만들어줘.
누락되거나 구현되지 않은 기능이 있으면 알려줘.
결과를 .specify/specs/001-todo-list/checklists/testing.md 로 저장해줘.
```

**터미널** 에서 확인한다.

```bash
cat .specify/specs/001-todo-list/checklists/testing.md
```

`[ ]` (미통과) 항목이 있으면 아래 순서로 처리한다.

```
1. Copilot Chat 에서 수정 방법을 물어본다
   예) "할일 추가 시 빈 제목 검증이 안 되는 것 같아. 어디를 수정해야 해?"
2. Copilot 에게 해당 컴포넌트/훅 파일 수정을 요청한다
3. 체크리스트를 재실행하여 통과 여부를 확인한다
   예) "/speckit.checklist testing"
4. 모든 항목 통과 → Part 6 (실행 및 테스트) 로 진행
```

> **확인 포인트 ✅**  
> `checklists/testing.md` 가 생성되고, 7가지 기능이 모두 통과 표시되면 Part 5 완료.

---

## Part 6. 결과물 실행 및 테스트

### 6-1. 의존성 설치 (implement 단계에서 자동 설치되지 않은 경우)

```bash
npm install
```

### 6-2. 개발 서버 실행

```bash
npm run dev
```

예상 출력:

```
▲ Next.js 14.x.x
- Local:        http://localhost:3000

✓ Ready in 1.2s
```

브라우저에서 `http://localhost:3000` 에 접속하여 화면을 확인한다.

### 6-3. 기능 수동 확인

| 확인 항목 | 방법 |
|---|---|
| 할일 추가 | 제목 입력 후 추가 버튼 클릭 → 목록에 즉시 표시되는지 확인 |
| 완료 토글 | 체크박스 클릭 → 취소선이 표시되는지 확인 |
| 삭제 | 삭제 버튼 클릭 → 목록에서 즉시 제거되는지 확인 |
| 필터링 | 전체/진행중/완료 탭 클릭 → 해당 항목만 표시되는지 확인 |
| 로컬 저장 | 할일 추가 후 새로고침(`F5`) → 목록이 유지되는지 확인 |
| 다크모드 | 우측 상단 토글 클릭 → 배경/글자색이 전환되는지 확인 |
| 반응형 | 브라우저 개발자도구(F12) → 기기 툴바에서 375px 로 확인 |

### 6-4. 자동화 테스트 실행

**단위 테스트 (Vitest):**

```bash
npm run test
```

예상 출력 (요약):

```
✓ tests/useTodos.test.ts (5)
✓ tests/TodoItem.test.tsx (3)

Test Files  2 passed (2)
     Tests  8 passed (8)
```

**E2E 테스트 (Playwright):**

```bash
npx playwright test
```

예상 출력 (요약):

```
Running 6 tests using 1 worker

  ✓ 할일을 추가할 수 있다 (1.2s)
  ✓ 할일을 완료 처리할 수 있다 (0.9s)
  ✓ 할일을 삭제할 수 있다 (0.8s)
  ✓ 필터가 정상 동작한다 (1.1s)
  ✓ 새로고침 후에도 목록이 유지된다 (1.4s)
  ✓ 다크모드가 전환된다 (0.7s)

  6 passed (6.1s)
```

> **확인 포인트 ✅**  
> 개발 서버가 정상 실행되고, 단위 테스트와 E2E 테스트가 모두 통과하면 Part 6 완료.

---

## Part 7. 버그 수정 작업 실습 (TDD 절차)

현재 `tasks.md` 에 등록된 **"[Bug] Fix: 다크모드 수동 전환 시 배경색 미적용 버그 수정"** 작업을  
TDD(Test-Driven Development) 절차로 진행한다.

### 버그 상황 재현

다크모드 토글 버튼을 클릭했을 때, 텍스트 색상은 바뀌지만 **배경색은 흰색(`#ffffff`)에 머물러 있는 상태**로 가정한다.  
아래 4단계로 이 버그를 고친다.

```
[Test] 실패하는 테스트 작성 (Red)
   ↓
[Verify] 테스트 실행 → 실패 확인
   ↓
[Fix] 코드 수정
   ↓
[Verify] 테스트 실행 → 통과 확인 (Green)
```

---

### Step 1. [Test] 실패하는 테스트 강화

**Copilot Chat** 에 아래를 입력한다.

```
/speckit.clarify
현재 다크모드 E2E 테스트가 텍스트 색상만 확인하고 있어서
배경색 버그를 잡아내지 못하고 있어.

E2E 테스트(tests/e2e/darkmode.spec.ts)에서
다크모드 토글 클릭 후 <html> 또는 <body> 요소의 실제 배경색이
검은색 계열(#0a0a0a)로 변하는지 getComputedStyle 또는
Playwright 의 toHaveCSS 매처로 검증하도록 테스트를 강화해줘.
```

파일 수정 제안이 뜨면 `Accept` 를 클릭한다.

**터미널** 에서 테스트 코드가 강화되었는지 확인한다.

```bash
cat tests/e2e/darkmode.spec.ts
```

예상 출력 (핵심 부분 예시):

```typescript
test('다크모드 전환 시 배경색이 검은색으로 바뀐다', async ({ page }) => {
  await page.goto('http://localhost:3000');
  await page.getByRole('button', { name: '다크모드 전환' }).click();

  const body = page.locator('body');
  await expect(body).toHaveCSS('background-color', 'rgb(10, 10, 10)');
});
```

---

### Step 2. [Verify] 테스트 실행 → 실패 확인 (Red)

**터미널** 에서 실행한다.

```bash
npx playwright test darkmode
```

예상 출력 (버그가 있는 상태이므로 **실패**해야 정상):

```
  ✘ 다크모드 전환 시 배경색이 검은색으로 바뀐다 (1.1s)

  Error: expect(locator).toHaveCSS(expected)

  Expected: rgb(10, 10, 10)
  Received: rgb(255, 255, 255)

  1 failed (1.3s)
```

> **왜 실패를 먼저 확인하는가?**  
> 테스트가 처음부터 통과하면 그 테스트가 버그를 제대로 검증하지 못한다는 뜻이다.  
> 반드시 **빨간불(Red)**을 먼저 확인한 뒤 다음 단계로 넘어간다.

---

### Step 3. [Fix] Tailwind CSS 설정을 고려한 코드 수정

**Copilot Chat** 에 아래를 입력한다.

```
constitution.md 의 [테스트 원칙]에 따라 TDD 로 버그를 수정 중이야.

tests/e2e/darkmode.spec.ts 테스트가 실패하고 있어.
다크모드 토글 시 배경색이 바뀌지 않는 원인을 찾아 고쳐줘.

[확인해줄 부분]
- tailwind.config.ts 의 darkMode 설정이 'class' 로 되어 있는지
- 다크모드 토글 시 <html> 태그에 'dark' 클래스가 실제로 추가/제거되는지
- globals.css 또는 body 태그에 dark:bg-[#0a0a0a] 같은 배경색 클래스가
  실제로 적용되어 있는지
```

Copilot 이 제안하는 수정 사항을 확인하고 `Accept` 를 클릭한다.

**터미널** 에서 수정 내용을 확인한다.

```bash
cat tailwind.config.ts
```

예상 출력 (핵심 부분):

```typescript
const config = {
  darkMode: 'class',
  // ...
};
```

```bash
grep -n "dark" app/globals.css
```

예상 출력 (예시):

```
3:body {
4:  @apply bg-white text-gray-900 dark:bg-[#0a0a0a] dark:text-gray-100;
5:}
```

---

### Step 4. [Verify] 테스트 재실행 → 통과 확인 (Green)

**터미널** 에서 다시 실행한다.

```bash
npx playwright test darkmode
```

예상 출력:

```
  ✓ 다크모드 전환 시 배경색이 검은색으로 바뀐다 (1.0s)

  1 passed (1.2s)
```

전체 테스트도 함께 재실행하여 다른 기능이 깨지지 않았는지 확인한다.

```bash
npm run test && npx playwright test
```

예상 출력 (요약):

```
Test Files  2 passed (2)
     Tests  8 passed (8)

  6 passed (5.9s)
```

브라우저(`http://localhost:3000`)에서 직접 다크모드 토글을 클릭해 배경색이  
검은색으로 바뀌는지 눈으로도 최종 확인한다.

> **확인 포인트 ✅**  
> Playwright 테스트가 Red → Green 으로 전환되고, 브라우저에서 눈으로도 확인되면 Part 7 완료.

---

## Part 8. 전체 산출물 구조 확인

**터미널** 에서 최종 산출물 구조를 확인한다.

```bash
find .specify/specs -type f
```

예상 출력:

```
.specify/specs/001-todo-list/spec.md
.specify/specs/001-todo-list/plan.md
.specify/specs/001-todo-list/tasks.md
.specify/specs/001-todo-list/checklists/testing.md
```

프로젝트 코드 구조도 확인한다.

```bash
ls app/ components/ hooks/ types/
```

예상 출력 (예시):

```
app/:
  layout.tsx  page.tsx  globals.css

components/:
  TodoInput.tsx  TodoItem.tsx  TodoList.tsx  FilterBar.tsx  DarkModeToggle.tsx

hooks/:
  useTodos.ts  useDarkMode.ts

types/:
  todo.ts
```

> **확인 포인트 ✅**  
> 아래 세 가지가 모두 확인되면 실습 완주다.
> 1. `.specify/specs/001-todo-list/` 아래 `spec.md`, `plan.md`, `tasks.md`, `checklists/testing.md` 가 모두 존재한다
> 2. `npm run test` 와 `npx playwright test` 가 모두 통과한다
> 3. 다크모드 버그가 TDD 절차(Red → Green)로 수정되었다

### 실습 전체 흐름 복습

```
[구현]
constitution(개발원칙 + 테스트원칙)
    → specify(001-todo-list) → plan → tasks → analyze → implement
    → checklist testing(constitution 기반)

[버그 수정 — Part 7, TDD]
clarify(테스트 강화) → 테스트 실행(Red 확인)
    → 코드 수정(Fix) → 테스트 재실행(Green 확인)
```

---

## 트러블슈팅

### Copilot Chat 에서 `/speckit.*` 명령어가 보이지 않는다

VS Code 가 `todolist-project` 폴더를 루트로 열었는지 확인한다.

```bash
pwd
# .../todolist-project 가 출력되어야 한다

code .
```

`.github/prompts/` 폴더가 실제로 존재하는지 확인한다.

```bash
ls .github/prompts/
```

폴더가 없으면 `specify init` 을 다시 실행한다.

```bash
cd ..
specify init todolist-project --integration copilot --force
cd todolist-project
code .
```

---

### Agent 모드가 보이지 않는다

Copilot Chat 패널 하단 드롭다운에서 `Ask / Edit / Agent` 중 `Agent` 를 선택한다.  
`Agent` 항목 자체가 보이지 않으면 VS Code 를 최신 버전으로 업데이트한다.

```
VS Code 버전 확인: Help → About → Version
최소 요구 버전: VS Code 1.90 이상
```

---

### `npm run dev` 실행 시 포트 충돌이 발생한다

이미 3000번 포트를 사용 중인 프로세스가 있는 경우다.

```bash
# macOS / Linux
lsof -i :3000
kill -9 [PID]

# Windows (PowerShell)
netstat -ano | findstr :3000
taskkill /PID [PID] /F
```

또는 다른 포트로 실행한다.

```bash
npm run dev -- -p 3001
```

---

### `npx playwright test` 실행 시 브라우저 오류가 발생한다

Playwright 브라우저 바이너리가 설치되지 않은 경우다.

```bash
npx playwright install
```

설치 완료 후 다시 실행한다.

```bash
npx playwright test
```

---

### `tailwind.config.ts` 에 `darkMode: 'class'` 설정이 없다

Copilot Chat 에서 명시적으로 요청한다.

```
tailwind.config.ts 에 darkMode: 'class' 설정을 추가해줘.
그리고 다크모드 토글 시 <html> 태그에 'dark' 클래스를
추가/제거하는 로직이 hooks/useDarkMode.ts 에 있는지 확인해줘.
```

---

### `/speckit.checklist testing` 결과가 너무 일반적이다 (constitution 원칙이 반영 안 됨)

constitution.md 의 `[테스트 원칙]` 을 명시적으로 언급하여 재실행한다.

```
/speckit.checklist testing
constitution.md 의 [테스트 원칙] 과 spec.md 의 수용 기준을 모두 참조해서
테스트 체크리스트를 다시 만들어줘.
Vitest 단위 테스트와 Playwright E2E 테스트 항목을 각각 구분해서 정리해줘.
```

---

### 채팅 한도 50회가 부족하다

Copilot 에게 직접 아티팩트를 읽어 구현하도록 요청한다.

```
(Copilot Chat 에서)
지금까지 생성된 spec.md, plan.md, tasks.md 파일을 읽고
남은 컴포넌트를 한 번에 구현해줘.
```

또는 채팅 대신 VS Code 의 인라인 코드 완성 기능을 활용한다.  
(인라인 제안은 코드 완성 카운터에서 차감되며 한도가 더 넉넉하다.)

---

### `localStorage` 저장이 동작하지 않는다 (새로고침 시 목록이 사라짐)

`useEffect` 의 의존성 배열 설정 오류인 경우가 많다. Copilot Chat 에서 요청한다.

```
hooks/useTodos.ts 에서 localStorage 저장이 새로고침 후
반영되지 않는 문제가 있어.
- 초기 로딩 시 localStorage 에서 값을 읽어오는 useEffect 와
- todos 상태 변경 시 localStorage 에 저장하는 useEffect 가
  각각 올바른 의존성 배열([]와 [todos])로 분리되어 있는지 확인해줘.
```

---

## 전체 명령어 요약표

| 단계 | 위치 | 명령어 또는 작업 |
|---|---|---|
| uv 설치 (Mac/Linux) | 터미널 | `curl -LsSf https://astral.sh/uv/install.sh \| sh && source ~/.bashrc` |
| uv 설치 (Windows) | PowerShell | `powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 \| iex"` |
| specify 설치 | 터미널 | `uv tool install specify-cli --from git+https://github.com/github/spec-kit.git` |
| 프로젝트 생성 | 터미널 | `specify init todolist-project --integration copilot` |
| 폴더 이동 | 터미널 | `cd todolist-project` |
| VS Code 열기 | 터미널 | `code .` |
| 명령어 파일 확인 | 터미널 | `ls .github/prompts/` |
| Copilot Chat 열기 | VS Code | `Ctrl + Alt + I` |
| Agent 모드 선택 | Copilot Chat | 하단 드롭다운에서 `Agent` 선택 |
| 원칙+테스트원칙 설정 (1회만) | Copilot Chat | `/speckit.constitution` |
| 요구사항 정의 | Copilot Chat | `/speckit.specify` |
| 기술 설계 | Copilot Chat | `/speckit.plan` |
| 작업 목록 | Copilot Chat | `/speckit.tasks` |
| 일관성 점검 | Copilot Chat | `/speckit.analyze` |
| 코드 생성 | Copilot Chat | `/speckit.implement` |
| 구현 후 품질 검증 | Copilot Chat | `/speckit.checklist testing` |
| 의존성 설치 | 터미널 | `npm install` |
| 개발 서버 실행 | 터미널 | `npm run dev` |
| 단위 테스트 실행 | 터미널 | `npm run test` |
| E2E 테스트 실행 | 터미널 | `npx playwright test` |
| 버그 재현 테스트 강화 | Copilot Chat | `/speckit.clarify` |
| 특정 테스트만 실행 | 터미널 | `npx playwright test darkmode` |

**TDD 버그 수정 순서 요약:**

```
[Test]   테스트를 강화하거나 새로 작성한다 (버그를 검증할 수 있도록)
   ↓
[Verify] 테스트를 실행하여 실패(Red)를 확인한다
   ↓
[Fix]    Copilot 에게 원인 분석과 수정을 요청한다
   ↓
[Verify] 테스트를 재실행하여 통과(Green)를 확인한다
   ↓
[Verify] 전체 테스트를 재실행하여 다른 기능이 깨지지 않았는지 확인한다
```

---

*본 실습 자료는 GitHub Spec Kit 공식 문서를 기반으로 작성되었습니다.*  
*참고: https://github.com/github/spec-kit*  
*GitHub Copilot Free 기준: 월 50회 채팅, 2,000회 코드 완성 (2026년 6월 기준)*
